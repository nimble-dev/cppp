% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeDiscrepancyCalculator.R
\name{buildDiscrepancyCalculator}
\alias{buildDiscrepancyCalculator}
\title{Build the calculator without compiling}
\usage{
buildDiscrepancyCalculator(model, discrepancies, simulation, paramNodes)
}
\arguments{
\item{model}{An uncompiled NIMBLE model.}

\item{discrepancies}{A \code{\link[=discrepancy]{discrepancy()}} specification, or a list of them.}

\item{simulation}{A \code{\link[=simulation]{simulation()}} specification.}

\item{paramNodes}{Character vector naming the model nodes to set from each
posterior draw.}
}
\value{
A list with the nimbleFunction \code{calcNF}, the expanded \code{paramNodes},
the \code{dataNodes} the dataset is written into, and the \code{discNames}.
}
\description{
The first half of \code{\link[=makeDiscrepancyCalculator]{makeDiscrepancyCalculator()}}. It checks the
specifications and builds the nimbleFunction that holds the loop over draws,
but does not compile it.
}
\details{
Kept separate so the nimbleFunction can be compiled together with the model
and the MCMC in one call, rather than in a call of its own. Hand the result
to \code{\link[=wrapDiscrepancyCalculator]{wrapDiscrepancyCalculator()}}, with \code{calcNF} replaced by the compiled
version if there is one.
}
\keyword{internal}
