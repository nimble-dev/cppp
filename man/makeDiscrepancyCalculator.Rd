% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeDiscrepancyCalculator.R
\name{makeDiscrepancyCalculator}
\alias{makeDiscrepancyCalculator}
\title{Build a discrepancy calculator}
\usage{
makeDiscrepancyCalculator(
  model,
  discrepancies,
  simulation,
  paramNodes,
  compile = TRUE
)
}
\arguments{
\item{model}{A NIMBLE model.}

\item{discrepancies}{A \code{\link[=discrepancy]{discrepancy()}} specification, or a list of them.}

\item{simulation}{A \code{\link[=simulation]{simulation()}} specification.}

\item{paramNodes}{Character vector naming the model nodes to set from each
posterior draw. These must appear among the column names of the draws.}

\item{compile}{Compile the model and the discrepancies? \code{TRUE} by default.}
}
\value{
A function of \verb{(MCMCSamples, targetData, control, ...)}, returning a
list of two matrices: \code{obs}, the discrepancies of \code{targetData}, and \code{sim},
those of the replicates. Both have one row per draw and one column per
discrepancy, named after the discrepancies.
}
\description{
Create a function that computes observed and replicated discrepancy values
for a set of posterior draws, from discrepancy and simulation
specifications.
}
\details{
For each posterior draw, the returned function sets the parameter nodes,
writes the supplied dataset into the data nodes, evaluates every
discrepancy, simulates a replicate dataset, and evaluates every discrepancy
again. The model is restored to its original state on completion.

The returned function is suitable as the \code{discFun} argument of
\code{\link[=runCalibration]{runCalibration()}} and \code{\link[=runCalibrationNIMBLE]{runCalibrationNIMBLE()}}. It may also be called
directly, to inspect discrepancy values before running a calibration.

\code{model} must be uncompiled, and should be an instance not in use elsewhere,
such as \code{model$newModel()}, since the calculator writes parameter values and
data into it.
}
\examples{
\dontrun{
calc <- makeDiscrepancyCalculator(
  model         = model$newModel(),
  discrepancies = list(discrepancy("mean"), discrepancy("deviance")),
  simulation    = simulation("conditional"),
  paramNodes    = c("mu", "sigma")
)

d <- calc(MCMCSamples, targetData = y)
head(d$obs)
colMeans(d$sim >= d$obs)   # posterior predictive p-value per discrepancy
}
}
\seealso{
\code{\link[=discrepancy]{discrepancy()}} and \code{\link[=simulation]{simulation()}} for writing the specifications,
\code{\link[=runCalibrationNIMBLE]{runCalibrationNIMBLE()}} for running a calibration.
}
