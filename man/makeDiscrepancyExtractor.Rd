% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeDiscrepancyExtractor.R
\name{makeDiscrepancyExtractor}
\alias{makeDiscrepancyExtractor}
\title{Read discrepancies the MCMC already computed}
\usage{
makeDiscrepancyExtractor(
  discrepancies = "discrepancy",
  obsSuffix = "_model",
  simSuffix = "_simulated"
)
}
\arguments{
\item{discrepancies}{Character vector of column stems. Each stem also names
the matching output column.}

\item{obsSuffix, simSuffix}{Character, appended to each stem to give the
observed and the replicated column.}
}
\value{
A function \verb{function(MCMCSamples, ...)} returning \code{list(obs, sim)},
each a draws x discrepancies matrix.
}
\description{
Placeholder, until the online route settles. When the discrepancies are
computed during the MCMC run as a derived quantity, the values are already
in the output and this reads them out, in the shape
\code{\link[=makeDiscrepancyCalculator]{makeDiscrepancyCalculator()}} returns.
}
\details{
With the stem \code{"discrepancy"} the columns read are \code{discrepancy_model} (the
observed side) and \code{discrepancy_simulated} (the replicate side). The naming
is not fixed yet, which is why the stem and the suffixes are arguments.
}
