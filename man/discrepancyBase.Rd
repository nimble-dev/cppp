% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builtinDiscrepancies.R
\name{discrepancyBase}
\alias{discrepancyBase}
\title{The shape every discrepancy has}
\usage{
discrepancyBase()
}
\value{
A nimbleFunctionVirtual, used as the \code{contains} argument.
}
\description{
A discrepancy is a nimbleFunction whose \code{run()} takes no arguments and
returns a single number. Declaring \code{contains = discrepancyBase} is what lets
the package hold several discrepancies together and compile them in one go.
}
\details{
Write your own like this:

\if{html}{\out{<div class="sourceCode">}}\preformatted{myDisc <- nimbleFunction(
  contains = discrepancyBase,
  setup = function(model, dataNodes, modelNodes) \{\},
  run = function() \{
    returnType(double(0))
    return(min(values(model, dataNodes)))
  \}
)
}\if{html}{\out{</div>}}

Then name it with \code{\link[=discrepancy]{discrepancy()}}: \code{discrepancy("min", fun = myDisc)}.
}
\seealso{
\code{\link[=discrepancy]{discrepancy()}}, \link{discrepancyBuiltins}
}
